# Sample Datasets

The workspace includes `genz_social_media_usage_1M.csv` at the project root. DataInsight Pro exposes it through the **Sample** button and the `POST /api/datasets/sample` endpoint.

You can add more local samples here and register them by changing `SAMPLE_DATASET_PATH` in `backend/.env`.

